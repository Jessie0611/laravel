//Get rout example    
Route::get('/', function () {
    return view('home');
});
Route::get('/contact', function () {
    return view('contact');
});

//Parameters using routes
Route::get('/portfolio/{firstName}/{lastName}', function ($firstName, $lastName) {
    return $firstName . " " . $lastName;
});

//named routes
Route::get('/test', function () {
    return "this is a test";
});

//grouped routes
Route::get('/portfolio', function () {
    return view('portfolio');
});
Route::prefix("portfolio")->group(function () {
    Route::get('/portfolio/company', function () {
        return view('company');
    });
Route::get('/portfolio/organization', function () {
        return view('organization');
    });
});

PHP Laravel Terminal Commands
npm : node.js package manager
npm install vite --save-dev   [[install pkg for vite which is a way to create a testing server]]
npx vite   // npx vite :  node.js pkg execution for running diff pkgs 
npx vite build

routes – handles http request 
has specific folder inside Laravel project   routes : web.php

use Illuminate\Support\Facades\Route;
     ^we want to grab a certain class call routes

6 Different Types Of Routes: POST, GET, PUT, DELETE, patch, options

Some build in model methods:  All(),  Find(), Create(), Update(), Delete()

php artisan make:model Post -mcr
this creates a model titled Post with a PostController and resources 
 to prevent Cross-Site Request Forgery (CSRF) attacks